package atlantis.map.region;

import atlantis.map.position.APosition;

public class ApproximateRegionCenter {
    public static APosition approximateFor(APosition position) {
        return null;
    }
}
